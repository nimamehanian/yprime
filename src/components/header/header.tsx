import styled from 'styled-components';

const HeaderWrapper = styled.div`
  height: 48px;
`;

function Header() {
  return (
    <HeaderWrapper>
      HI
    </HeaderWrapper>
  );
}

export default Header;
